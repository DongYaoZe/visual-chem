# VisualChem 2.0 落地进展

> 更新：2026-07-16  
> 当前里程碑：第一篇故事 Alpha + 常压实验证据层，可本地运行、静态构建与浏览器验收；尚未达到公开 Beta。

## 已完成

### 产品与叙事

- 建立 SvelteKit 5 + TypeScript + MDsveX 的静态站点，可零服务器部署到 GitHub Pages。
- 完成首页的编辑型视觉系统，不沿用旧 Streamlit 控制面板语言。
- 完成《永远到不了的 100%》九幕滚动原型：预测、组成定义、首泡、x/y 结线、T-x-y 图、理想级联、非理想模型、不动点与选择性脱水反转。
- 完成“亲手重建相图”一幕：读者从 Lai 2014 的 16 组真实文献数据中选择至少 5 组，先画自己的边界草图，再展开完整实验点与模型对照。
- 提供作者控镜后的自由探索区；读者可调初始组成、理论级数与非理想强度。
- 明确显示“理论平衡级”“教学模型”“微观轨迹为示意”等证据边界。

### 三重表征组件

- `DistillationApparatus.svelte`：宏观烧瓶—冷凝—接收装置，组成和泡点同步。
- `ParticleChamber.svelte`：液/气两相的乙醇/水比例同步；粒子位置与运动不冒充分子动力学。
- `TxyDiagram.svelte`：实验点及不确定度、用户重建草图、模型泡/露点线、结线、实验/模型共沸点与逐级精馏阶梯；形状和线型不只依赖颜色。
- `TriView.svelte`：一个状态源驱动三幅图，并支持叙事幕聚焦与降噪。
- `ConceptCheck.svelte`：先选择预测，再给因果解释。

### 计算与验证

- 从 `app.py` 抽出 Antoine、三后缀 Margules、泡点、露点、共沸、flash、组成换算与理论级联的无 UI TypeScript 内核。
- 发现并隔离旧乙醇—水预设的定量错误：旧模型给出约 `89.0 wt%` 共沸，新 Alpha 校准到约 `95.6 wt%`。
- 从 NIST ThermoML 原始 JSON 落地 Lai 2014 的 16 组常压 `x–T–y` 数据、逐点 95% 扩展不确定度和独立共沸点；页面物理事实统一写“约 `95.5 wt%`”。
- 将 Kamihama 2012 的另一实验室 21 点作为独立验证集：与 Lai 的共同网格差异为 `0.224396 K / 0.0077243 y`；验证资产不接入故事主曲线，也不与校准集混合。
- 量化而不隐藏模型偏差：当前 Margules 相对 16 个实验点的温度 RMS 约 `0.57 K`、气相组成 RMS 约 `0.016`。
- 当前 Margules 在独立 Kamihama 21 点上的 RMS 为 `0.777318 K / 0.0197836 y`。
- 用独立 SciPy `brentq` 脚本生成 10 个跨语言 golden points，Vitest 对照温度、气相组成与活度系数。
- 已覆盖共沸不动点、纯组分、露点直接求解、杠杆规则闭合和逐级精馏不穿越共沸点。

### 工程质量

- `eslint + prettier + svelte-check + Vitest + Playwright + axe` 全链路。
- Vitest 当前 12 项，覆盖数值内核、跨语言基准、实验数据完整性和模型残差区间。
- Playwright 当前 10 个桌面/移动场景，覆盖首页入口、故事加载、预测、理论级、5 组文献数据重建及首页/故事的严重无障碍违规扫描。
- 当前源码另做 6 组 Axe 人工触发扫描（首页/故事 × `1440×900`、`412×915`、`320×568`），总 violation 均为 0；短屏弹层的旋转自动关闭、Back 后滚动恢复、键盘录入和焦点返回均已实测。
- `412×915` 第 04 幕相图与文字卡净空约 `23.9 px`、重叠为 0；`320×568` 用可关闭的“查看当前图”弹层保留完整图形。
- `scripts/capture-audit.mjs` 一次生成桌面/手机首页、故事首屏、实验重建完成态与实验—模型对照截图。
- GitHub Actions 在 `main` 的 `web/**` 改动上验证并构建，支持 `/visual-chem` 子路径后部署到 Pages。

## 当前目录

```text
visual-chem/
├─ app.py                         # 旧 Streamlit 参考原型，保留但不再是产品主入口
├─ validation/
│  └─ generate_ethanol_water_reference.py
├─ web/
│  ├─ src/lib/chem/               # 浏览器端热力学内核与测试
│  │  └─ data/                    # NIST ThermoML 实验数据、来源与完整性测试
│  ├─ src/lib/components/         # 三联动视图与教学组件
│  ├─ src/routes/                 # 首页与故事长文
│  └─ scripts/capture-audit.mjs   # 视觉审计截图
├─ docs/
│  ├─ 化学叙事可视化蓝图.md
│  ├─ 第二轮定向调研.md
│  └─ 乙醇水数值模型卡.md
└─ .github/workflows/deploy-pages.yml
```

## 尚未完成，不能提前宣称

### 公开 Beta 的硬门槛

1. **真实学习测试**：至少 5 名目标学生进行出声思维测试，记录卡点、错误概念、完成时间和前后测，不以开发者自评代替教学证据。
2. **无障碍人工验收**：自动化 Axe 严重/致命违规已归零，键盘、短屏弹层、视口旋转和移动遮挡已做专项回归；仍需真实读屏器与多种浏览器的人工检查。
3. **内容审校**：由至少一名物理化学教师审查公式、实验引用、模型边界与“精馏级”措辞。
4. **国际发布**：英文共享逻辑、OpenGraph 分享图、版本化引用和离线/无 JS 降级已进入代码；仍需完整生产回归、提交/tag/Release 和真实 Pages URL 验收。

### 第一季后续

- 《沸腾的地图》：水/CO₂ 单组分 p-T 图与 Clausius–Clapeyron。
- 《一锅盐水的分身术》：三元水盐相图与蒸发结晶轨迹。
- 把首篇反复出现的状态、Scrolly、三联动视图和概念题抽成 `story-kit`，再评估独立 npm 包。

## 本地验收

```powershell
npm --prefix web install
npm --prefix web run validate
npx --prefix web playwright install chromium
npm --prefix web run test:e2e
```

视觉验收需先启动开发服务器：

```powershell
npm --prefix web run dev -- --host 127.0.0.1
npm --prefix web run audit:visual
```

截图输出到 `web/test-results/`，该目录不提交 Git。
